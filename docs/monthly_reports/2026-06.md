# Monthly Tool Analysis — 2026-06

**Verdict:** `[AMBER]` — system healthy, learning-loop plumbing sound, spend near zero, memos accurate. The one flagged RED-line crossing (memo top-5 hit-rate 0.444) was **investigated and dismissed as a measurement artifact** by two independent agents. Genuine things to watch: a **new gauge era** (2026-06-27, `5687885c`) that tightened concentration caps (early read positive but tape-confounded), a **real `high_theme_confidence` signal regression**, and two AMBER instrumentation items (62% untagged signals; a self-invalid memo-hit-rate metric to retire).

**Run:** 2026-07-01 (covers rolling 30d, 2026-06-01 → 2026-07-01). Prior baseline: 2026-05 (AMBER).

---

## Lead line

```
[AMBER] monthly tool analysis 2026-06: new gauge era 2026-06-27 tightened caps (position 0.15→0.12,
        sector 0.35→0.25), current era hit 0.64 (sig. vs prior, tape-confounded); memo 44.4% proxy
        debunked as join artifact (memos GREEN/accurate); high_theme_confidence real -28.7pp regression;
        cron 31/30, spend ~$0, drift 0%, quarantines 0, rotation 0.94. Watch: 62% untagged, retire memo proxy.
```

## Headline metrics

| Lens | Metric | Value | Status |
|---|---|---|---|
| Developer | cron_health | 31/30 archives w/ decision_plan | GREEN |
| Developer | apply_rate_per_week | 0 (audit log absent; monthly_drift {}) | AMBER (dormant, benign) |
| Developer | rollback_ratio | 0/0 (no applies) | GREEN |
| Developer | pulse_skip_rate | 0/104 = 0% | GREEN |
| Quant | winning_tag_count | 1 (sector:Financial_Services +15.5pp) | GREEN |
| Quant | new_winners_this_month | 0 (Financial_Services persisted) | AMBER |
| Quant | tag_efficacy_drift | high_theme_confidence −28.7pp (winner→strong_loser) | AMBER (≥10pp swings) |
| Quant | fingerprint_stability | 4 days (5687885c, first 2026-06-27) | new gauge era |
| Quant | oos_window_maturity | 64/315 cal days, folds_possible=false, ETA 2027-03-09 | GREEN (accruing) |
| Quant | calibration | inverted=true, slope −11.345, apply_gate=oos_unconfirmed | AMBER (provisional) |
| Quant | tagging untagged_pct | 0.6222 (62%) | AMBER (high_untagged_rate) |
| Process | drift_cap_utilization | 0% (monthly_drift {}) | GREEN |
| Process | pending_confirmation_age_max | 2 queued, age indeterminate (state 06-29) | GREEN |
| Process | pulse_cost_pace | ~$0.0/mo (openai $0, ai_budget $0.0004) vs $20 cap | GREEN |
| Process | operator_worker_throughput_30d | 9 terminal (3 completed, 6 failed; 5 cancelled) | observe-only |
| Process | operator_quarantine_count_30d | 0 | GREEN |
| Process | operator_decision_queue_age_max | 0 (none awaiting_approval) | GREEN |
| Market | memo_top_decision_hit_rate | 0.444 (n=36) — **invalid metric** (join artifact) | not a finding |
| Market | universe hit_rate_1d | 0.5087 (n=804), mean 1d −0.15% | AMBER (coin-flip) |
| Market | current-era hit_rate_1d (5687885c) | 0.64 (n=75), mean 1d +1.67% | GREEN |
| Market | net_new_discovery_yield | 4 tickers positive (CVX/NOC/RTX/XOM), 19 hits/56 | GREEN |
| Market | sector_rotation_score | 0.943 normalized entropy (8 sectors) | GREEN |
| Market | regime_consistency | 100% neutral (degenerate; known live collapse) | (no diversity) |
| Market | decision mix | BUY 43% / WAIT 37% / actionable-balanced | GREEN (vs 73% WAIT last mo) |
| Sandbox | portfolio_backtest | ok, result_count 192 | GREEN |
| Sandbox | strategy_lab | AMBER — research_momentum_rotation still_works_oos=false | AMBER |
| Sandbox | strategy_catalog | coverage_complete=true | GREEN |
| Docs | doc_audit | coverage_gap — 2 undocumented modules, 1 drift auto-fix pending | AMBER |

---

## 1. Developer lens

Cron health: **31/30 days archived** (2026-06-01 → 2026-07-01, every day has `decision_plan.json`). Apply rate **0/wk** — `data/retune_audit_log.jsonl` is absent and `retune_auto_apply_state.monthly_drift` is `{}`, so nothing auto-applied this month (**0/30 rollbacks**). Pulse skip rate **0%** (0 skipped of 104 runs) — the 6.7% skip pressure from last month cleared. Learning-loop dormancy is benign this month (2 auto-applicable proposals are mid-confirmation, not blocked).

## 2. Quant lens

Tags this month: **1 winner** (sector:Financial_Services, strong_winner +15.5pp — persisted from prior week, not newly promoted), **9 losers** (5 strong_loser + 4 loser), **9 neutral, 1 insufficient**. **0 new winners promoted.** Match-rate 0.87.

Notable drift (weekly→monthly `vs_baseline_pp`): `high_theme_confidence` **+6.78 → −21.9 (−28.7pp)** flipping winner→strong_loser; `source:fmp_top100` −21.75 → +2.47 (+24pp); `sector:Technology` −15.5 → −0.04 (+15.5pp). The theme-confidence deterioration is echoed by the loser cluster `source:theme_candidate` (−14pp), `net_new_discovery` (−29.6pp), `source:extended_watchlist` (−27.6pp).

Pattern-Loop OOS window: **64/315 cal days, folds_possible=false, first full window ~2027-03-09**. Proposals: **0** (`insufficient_evidence`, oos_n=0) — EXPECTED and healthy while pre-maturity; accruing. `performance.evaluated=858`, per_regime carries 5 distinct labels (not degenerate).

Feedback proposers — **calibration: inverted=true** (slope −11.345; 80-100 conf band 60.6% hit vs 40-60 band 83.3% — genuinely inverted, but `apply_gate=oos_unconfirmed`, PROVISIONAL/in-sample — surfaced AMBER, not dispatched per skill's D-proposer rule). **Tagging: 62% untagged** (924/1485), families missing registry id: `[SIGNAL_SCORE]` — high_untagged_rate; route the tagging proposal (registry entry + backfill spec) to the owner; it improves attribution now.

## 3. Process analyst lens

Drift cap max **0%** on all params (`monthly_drift` empty). Pending confirmations: **2** (`sanitation_weight.hit_rate`, `sanitation_weight.sources`) — both auto_applicable, awaiting their 2nd confirmation run; state regenerated 2026-06-29 so age is short. Burn pace **~$0.00/mo** vs $20 cap (OpenAI month cost $0.0; ai_budget monthly $0.0004 — the AI decision-validator is the only spender, in cents).

Operator-control: **9 worker runs reached terminal** in 30d (3 completed, 6 failed), 5 cancelled; **0 quarantined** (no `worker_protected_path_violation`); decision queue oldest **0d** (nothing awaiting_approval). One `worker_production_impact` + one `deploy_update_applied` event recorded (operator-driven, observe-only). Inert/healthy on the autonomy axis.

## 4. Market expert lens

Memo top-5 decision hit-rate computed at **44.4%** (16/36) — this crossed the RED line, **but the memo-reviewer and attribution-analyst both showed the metric is invalid as computed** (an exact-date top-5→scan-outcome inner join that scores a biased ~23% fresh-scan subsample; the memo's rendered "Top Decisions" is a `source=='market'` list, not a raw-priority list, and 77% of top decisions are persistent holdings that never re-appear as scan signals). The correctly-computed shipped hit-rates are healthy: universe 30d **50.9%** (n=804, mean 1d −0.15%), and the **current gauge era (post-06-27) is 64%** (n=75, mean 1d +1.67%). The memos themselves reviewed **GREEN** (accurate, coherent, contract-compliant). Net-new discoveries: 56 discovery-sourced signals resolved, **19 hits across 4 distinct tickers** (CVX, NOC, RTX, XOM) — energy + defense working. Sector rotation **0.943** (8 sectors, high diversity — no lock-in). Dominant regime: **neutral 100%** (degenerate; the known live neutral-collapse — classifier validated healthy in sim, live tape genuinely calm + immutable pre-fix rows). Decision mix rebalanced sharply: **BUY 43% / WAIT 37%** vs last month's 73% WAIT.

Sandbox sim suite (observe-only): backtest ok (192 results, top excess-vs-SPY tactic "Boom Bucket" +70%), projection ok (p50 CAGR 14.5%, monotone percentiles), strategy_catalog `coverage_complete=true`. Strategy Lab **AMBER** — `research_momentum_rotation` surfaced with `still_works_oos=false` (26 tactics, factor data available, otherwise healthy).

## 5. Notable trends vs last month

- **Decision mix rebalanced** — WAIT collapsed 73.4% → 37%, BUY rose to 43%. The system is acting, not just deferring. Worth confirming the added BUYs are earning their keep (early: current era hit 0.64).
- **Gauge era flipped twice** (e2b5 06-13 → 5687885c 06-27); the latest **tightened concentration caps** (position 0.15→0.12, sector 0.35→0.25, max_sector_alloc 0.10→0.06). This single change explains three otherwise-separate findings: the 4-day fingerprint age, the `ALLOCATION_POLICY.md` doc drift (doc 0.35 vs source 0.25), and the era-conditional attribution split.
- **Theme/discovery signals deteriorated** — `high_theme_confidence` flipped winner→strong_loser (−28.7pp), and the whole theme-candidate/extended-watchlist/net-new-discovery cluster is red on the monthly window, even as those sources still produced 4 profitable tickers.

## 6. Agent dispatch results

- `portfolio-learning-loop-health` (tag drift ≥10pp): **AMBER** — the `high_theme_confidence` winner→strong_loser flip is a **real signal-quality regression, not sampling noise** (n=167, Wilson CI [0.175, 0.314], monotone decay 23.8%→15.4%→7.6% over 1d/3d/7d, mean 7d −5.08%), structurally tied to the `promoted_to_extended` loser cluster (hit_rate_7d=0.0). The weekly +6.78 "winner" read (n=47) was the artifact. Loop is correctly conservative: only *neutral* nudges are auto-applicable; the strong loser cluster is gated out of auto-apply. Missing audit log = expected (loop never applied). Minor observability gap: pending_confirmations carry no timestamp, so a genuinely-stuck confirmation can't be distinguished from state alone.
- `portfolio-attribution-analyst` (new gauge era + memo underperformance): **OBSERVE / no gauge action.** Current era 5687 hit 0.640 (n=75), **significantly better than the degraded mid-eras** (vs e2b5 clean caps-only delta +16.6pp, p=0.008; decision-ledger +31pp p=0.002) and **statistically tied with the best prior era f60e** (p=0.42) — the 0.64 is not noise. The **0.444 memo proxy is a stale all-era composite dominated by the degraded d95e+e2b5 eras**; the current gauge is running at ~its best-ever level, NOT underperforming. Caveat: **favorable-baseline trap** (2026-06-04 pattern) — f60e (loose caps) also ran ~0.69 on a favorable tape, so the caps tightening did not hurt but the tape is doing real work; regime lane can't disambiguate (neutral-collapse). Persistent gauge-independent drag: **Energy (0/9, −1.03%) + Financial Services (0.25, −2.69%)** — a sector-selection problem, the natural next retune target (NOT a caps revert). Next checkpoint: 5687's 3d/7d resolutions ~07-05, n≈100 by ~07-11.
- `portfolio-memo-reviewer` (memo hit-rate < 0.50): **GREEN — memos mechanically accurate, coherent, contract-compliant** (every figure spot-checked against source JSONs; only a cosmetic 17-digit-float nit at `daily_memo.md:108`). Crucially: **the 0.444 proxy is structurally invalid** — (1) the join key is wrong (memo "Top Decisions" is a `source=='market'` top-5, not a raw-`priority_score` top-5; today it omits AMD @0.774, the highest-priority BUY); (2) 77% unmatched-rate (119/155) is a population mismatch — memo lead decisions are persistent holdings/rebalance targets that never reappear as fresh scan signals, so the inner join scores a biased ~23% subsample (n=36, CI ±0.16); (3) the system already computes the fingerprint-attributed hit-rate correctly (0.64 current era) and it disagrees with the proxy. **Recommendation: retire the exact-date top-5→scan-outcome join; grade the fingerprint-attributed resolved set (`retune_impact.json` / `decision_outcomes.jsonl`) segmented by source instead.**
- `portfolio-doc-writer` (always — persist report + roadmap/state): _pending_
- Documentation lens (`doc_audit_status.json`): overall_status **coverage_gap** — 2 undocumented modules (`opportunity_decisions.py` → `docs/opportunity_decisions.md`; `strategy_selection.py` → `docs/strategy_selection.md`), 1 auto-fixable drift pending (`ALLOCATION_POLICY.md` sector_cap 0.35→0.25, downstream of the 06-27 gauge change), 0 auto-fixes applied.

## 7. Operator action queue (this month)

1. **Observe the 06-27 gauge era at the 3d/7d checkpoints** — attribution-analyst found it significantly better than the degraded mid-eras (p≤0.01) but tape-confounded. Re-check when 5687's 3d/7d resolutions land (~07-05) and again at n≈100 (~07-11). No gauge action now. The persistent, gauge-independent **Energy + Financial Services sector drag** is the natural next retune target — NOT a caps revert.
2. **Route the tagging proposal to the owner** — 62% of signals are untagged (`UNKNOWN` family), starving per-pattern attribution. The proposed `SIGNAL_SCORE` registry entry + `alert_basis` backfill rule are review-only specs; approving them improves attribution now, ahead of OOS maturity.
3. **Fix the instrumentation, not the memos** — (a) retire the `memo_top_decision_hit_rate` exact-date join from this skill (it's a self-invalid metric; grade the fingerprint-attributed set instead); (b) apply the `ALLOCATION_POLICY.md` sector_cap doc drift (0.35→0.25) + document `opportunity_decisions.py` / `strategy_selection.py` via `/doc-audit`; (c) review the `high_theme_confidence` / `promoted_to_extended` theme-promotion reinforcement gate — a real −28.7pp regression the loop correctly gated out of auto-apply.

---

_Observe-only. This report reads artifacts and dispatches read-only diagnostic agents; it mutates no decision artifacts. Calibration inversion and OOS proposal-count=0 are pre-maturity states, not failures. Auto-apply remains inert (no `auto_apply_audit.json`), expected until ~2027._
